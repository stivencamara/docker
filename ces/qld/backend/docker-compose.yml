version: '3.9'
name: backend
services:
  api:
    container_name: api
    image: cesazores/ces:qld_api_1
    ports:
      - 5002:5002
      - 443:443
    restart: always
    networks:
      qld_ces_network:

networks:
  qld_ces_network:
    driver: bridge
    #external: true